# Tests

Unit tests for the InvoiceKit Swift package live under `InvoiceKit/Tests`. Run them with:

```sh
swift test
```

This directory is reserved for additional integration, UI, or end-to-end tests that may be added in the future.
